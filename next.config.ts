import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['makeitfable.com','logos-world.net','pngimg.com','logowik.com','i2idirectmarketing.com','factsfive.com',], // Add the hostname here
  },
};

export default nextConfig;
